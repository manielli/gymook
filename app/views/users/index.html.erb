<h1> List of the users: </h1>

<ul class="user-list">
    <% @users.each do |user| %>
        <li>
            <%= link_to(user.full_name, user_path(user.id), class: "text-secondary") %>
            
        </li>
    <% end %>
</ul>